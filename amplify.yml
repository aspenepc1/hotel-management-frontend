version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g npm@6
        - npm install
        - npm install leaflet
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
